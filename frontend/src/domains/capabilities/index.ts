export type AppCapabilities = {
  isPremium: boolean;
  people: boolean;
  ownership: boolean;
};

export const capabilities: AppCapabilities = {
  isPremium: false,
  people: false,
  ownership: false,
};
